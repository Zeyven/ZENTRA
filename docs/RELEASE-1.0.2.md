# V1.0.2 点钟王预留入口发布

2026-09-09，用户授权发布云端及 Windows 安装包。

- 新增门店设置 → 设备接口 → 房间触控面板（点钟王），保存名称、型号及预期连接方式，默认待厂商确认。每店一份准备配置，保留商家/门店隔离。
- 未接入时测试返回 ROOM_PANEL_NOT_CONNECTED，不产生设备通信或上下钟业务。
- 修复设备数据首次加载完成前可以打开空配置表单的问题。
- 全部工作区及 API 版本升至 1.0.2，保留原 1.0.1 安装包，避免同版本覆盖无法被更新器发现。

## 部署证据

- 发布目录 /opt/za-spa-saas/releases/20260909T064615Z，归档 SHA256 e73258b86b245961b5fbe694255ce594b3e2080215298b8fb2a62968f3777ff1。
- 迁移前备份 za_spa_saas-20260909T064557Z-ff6e6142.dump，SHA256 5a728f9cde406d9506064f82f455f862150b32b1479dbc34d7df9e0328b29f3f。
- 生产已原子执行 029-room-panel-reservation.sql，原数据和 RLS 保留。应用回滚可保留该增量约束；无需删除设备准备配置。
- API /ready 返回版本 1.0.2 且 healthy；Nginx 配置通过；线上 API 和 Web 资产包含 room_panel。
- 旧 zuyu 服务维持发布前 inactive 状态，没有修改或启动旧服务。

## 验证

类型检查和全量构建通过；设备相关 API 4/4、设备浏览器保存刷新 1/1、自动更新控制器 2/2、Electron 独立测试启动 1/1 通过。线上平台 6 个页面通过，未产生业务写入，无 API/页面错误。

安装包 30 个内置文件与构建输出一致，更新清单 SHA512 匹配；经公开 HTTPS 实际下载完整安装包，112951394 字节，SHA256 2f8b770a8b691e1280eb09e60e1a21ba4eb64c084f151a79c48eef0be69d9df7。

本轮没有重新执行 NSIS 安装/升级全过程；Electron 启动验收使用隔离测试目录，不打断用户正在运行的客户端。没有点钟王或读卡器实机联调，预留配置不能代替厂商协议适配。

## 随后现场安装阻塞处理

用户报告交互安装器“无法关闭”后，确认客户端进程已退出，重试仍停留在关闭检查。安装包 SHA256 与发布一致；实际捕获的 PowerShell 检测路径为 D:\ZAThera\ZA-Thera，独立执行该检测以及最小 NSIS 探针均返回未运行。结束已阻塞的安装器后，用原校验安装包的受支持 /S 模式安装至原目录，返回 0；已安装版本 1.0.2，app.asar 与发布包逐字节一致，客户端窗口已启动。没有修改安装器源码；交互关闭循环内部根因尚未确认，不能将本次恢复称为永久修复。记录 .runtime/install-recovery-1.0.2.json。
