# 会员日期与等级

2026-09-08，测试库迁移至 018（后续设备迁移为 019）。生日与有效期验证真实日期，包括闰日；省略日期不清空原值，显式空串改为长期或未设置。会员页面增加有效期编辑和详情展示。沿用原系统到期提醒语义，未擅自增加到期后余额冻结或清零。

等级按会员关联的已结账订单净应收累计，结账及反结账都在原事务内重算，独立于赠积分数量；通用会员使用开卡门店的等级门槛，其他门店不能通过自己的等级配置覆盖它。保留人工指定的最低等级，自定义名称没有门槛时保持不变。等级变动有审计，不自动更改会员个人折扣，符合原等级变更行为。

日期用例 1/1 通过；等级与钱包回归 8/8 通过，覆盖两店并发、源店等级策略、反结账重算、手工等级保留和原资金分配回滚。首轮新增代码因编辑时 SQL 占位符丢失导致会员创建失败，已恢复参数绑定；随后等级用例因遗漏技师而不能添加服务，改为使用真实商品交易验证消费门槛，未绕过钟务校验。

会员编辑的其他字段并发版本控制和重启后的未确认请求恢复仍须继续审查，不能把本记录视为全部会员功能或上线验收通过。
