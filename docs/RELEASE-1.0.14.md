# ZA Thera｜澜序 1.0.14

2026-09-15，发布 API、Web 与 Windows 客户端 1.0.14。

- 依据用户提供的当前固件日志，网关新增只读 `SystemClockInfo` 响应：映射房态 `空闲、占用、待打扫、维修、已预订`，以及不含顾客和支付数据的钟列表 `t1` 至 `t5`。
- 系统版本显示统一为 1.0.14，修正运行日志和技术后台运行状态仍显示旧版的问题。
- 点钟、加钟、落钟、技师验证和钟房播报仍拒绝执行，等待实机设备注册、菜单与写入回执验证；本版不将日志证据表述为实机互通。

验证：硬件网关相关测试 13/13 通过；全项目 TypeScript 类型检查、服务端构建、Web 构建、Windows 打包和 31 个受检打包文件核验通过。Windows 包 `ZA-Thera-1.0.14-Windows-x64.exe` 为 112,976,487 字节，SHA-256：`e1fb2115af37d3d4d3088f0cb56afa11304e53b494796092bd98c85964fa690d`。

本版无数据库迁移。真实面板/钟房机顶盒/播报器的现场验收尚未完成。
