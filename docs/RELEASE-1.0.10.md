# V1.0.10 发布与验证

2026-09-15，Web、API、Windows stable 和 rc 更新渠道已发布。独立发布目录为 `/opt/za-spa-saas/releases/20260915T064253Z`，前一版本保留在 `/opt/za-spa-saas/releases/20260915T011440Z`。发布时没有数据库迁移；发布后发现生产库缺少既有的 `034-hardware-binding-uniqueness.sql`，已在无绑定数据、无冲突记录的前提下补齐。发布前数据库备份为 `/opt/za-spa-saas/backups/pre-1.0.10.dump`，补迁移前备份为 `/opt/za-spa-saas/backups/pre-034-hardware-binding-uniqueness.dump`；两份均可由 `pg_restore --list` 读取。未进行完整恢复演练。

本版将服务端、接口包、Web 和 Windows 源码版本统一至 1.0.10，并使本机点钟王只读网关在启动时核验对应业态的原厂授权菜单。只有可读缓存但缺少该业态菜单时，不再显示“已加载”或开启登录准备状态。该改动不开放报钟写入，也不构成实机协议兼容声明。

验证结果：工作区类型检查及构建通过；Windows 包内 31 个文件与构建产物一致，更新元数据匹配；硬件网关 22 项测试通过。使用本机隔离目录执行 1.0.9 → 1.0.10 安装、升级、卸载，各进程退出码均为 0，安装后的 `app.asar` 与新包一致；原有 1.0.3 安装、注册信息、快捷方式和用户数据已恢复。该测试不覆盖用户点击安装向导和真实客户端自动下载、安装、重启全过程。

安装包 `ZA-Thera-1.0.10-Windows-x64.exe` 为 112,976,145 字节，SHA256 为 `9182f698c30d062824ad494d8ff5d5b375ad53cb34f3c14300177c85cb7e2eb7`。公网 stable 下载文件 SHA256 与本地完全一致；stable/latest.yml、rc/rc.yml 均为 1.0.10，清单 SHA512 与安装包一致；固定下载别名返回 200 和正确大小。公网首页入口资源 `index-BeIX9Z_g.js` 与本地 Web 构建哈希、字节数相同。公网 `/ready` 返回 1.0.10，未认证网关管理接口返回 401，SaaS systemd 服务 active，Nginx 配置检查通过。旧 `zuyu` 服务在发布前后均为 inactive；本次发布未操作旧服务。SaaS 服务配额维持 768 MiB 和 CPU 60%。

当前限制：安装包未签名；真实 7 寸面板的菜单业务映射、设备注册授权、报钟写入和钟房播报仍缺厂商协议或实机证据。生产网关继续拒绝未验证的写操作，不能声称全部设备一键配置或正式互通已经完成。后续源代码还收紧为仅接受现场样本所示的 Thrift 单向调用类型；该代码变更已通过 22 项网关测试和类型检查，但尚未作为新安装包发布。

回滚点：旧发布目录、`stable/pre-1.0.10.yml`、`rc/pre-1.0.10.yml` 和 1.0.9 安装包均保留。服务回归时先切回旧目录并重启 SaaS；更新渠道需要同步恢复两个清单与下载别名。已安装 1.0.10 的客户端不会被自动降级。
