# V1.0.0 正式版发布记录

2026-09-09：Web、服务端及 Windows 统一发布 1.0.0。生产发布目录标识为 20260909T030236Z。

Windows 正式渠道为 `/updates/windows/stable/latest.yml`。旧 rc 渠道同时提供 1.0.0，旧客户端升级后转为正式渠道。正式客户端禁止预发布与降级；继续自动检查、用户确认下载和安装。

安装包：ZA-Thera-1.0.0-Windows-x64.exe，112950836 字节。
SHA-256：b8107c762f588a2b20ec768d521f2c5ff7bac6b951db057c3fa9b5923e6e6e5c。

## 本轮验证

- 类型检查、服务端/Web/Electron 构建及 NSIS 打包成功；29 项包内文件和更新元数据校验通过。
- 更新、安全、请求错误共 5 项 Node 测试和 5 项 Web/Electron 测试通过。
- 生产平台六工作区、登录退出及移动宽度检查通过；API 版本为 1.0.0，数据库、任务、备份健康。
- 正式和 rc 两个公开更新清单均为 1.0.0，安装包 HTTP 200、大小正确；上传逐文件核对 SHA-256，清单最后替换。
- 隔离安装 rc.7 后实际升级至 rc.8，再升级至 1.0.0。观察到从更新缓存启动的 1.0.0 安装向导；升级后的安装注册与 app.asar 均为 1.0.0，实际客户端面板显示“当前已是此渠道的最新版本”。期间用户也操作了安装窗口，因此这不是完全无人值守的 UI 自动化测试。
- 测试配置标记经过两次升级保留。测试安装卸载退出码 0、注册项移除；原用户目录和两个快捷方式恢复，原 D 盘便携客户端哈希未改变。证据在 `.runtime/install-retest-rc8/restoration-result.json`。
- 新旧服务健康；旧项目 93 个源文件无变化。

前序完整业务回归和容量结果见 AUDIT-2026-09-09.md，本轮未重复全部业务测试。实际支付、团购及硬件仍按用户要求仅预留接口。安装包当前没有代码签名证书，不声称已签名或所有 Windows 环境均完成验证。
