# 2026-09-18 硬件验证修正

重新读取用户提供的 Hardware.zip 内 Hardware/log/Commlog.2026-09-13.txt，仅统计 HeadBeat 请求解码后正文：空字节 108031 条、JSON 字符串 0 条、其他 0 条。此前将日志 base64 字段的字符串类型误当成解码后正文类型，导致合法心跳被 JSON.parse 拒绝并断开 TCP。

修正：HeadBeat 接受零字节 Buffer；非空正文拒绝；心跳不建立业务授权。TCP 回归使用只有 MSG_ID/from_session 的匿名合成头和空正文，随后发送房态请求，确认仍可返回且心跳未调用业务处理器。

房态转换额外拒绝未知钟状态和继承属性名称，避免将异常钟状态默认为“服务中”。

本轮为源码修复及本机验证，未发布新版本，未完成真实面板或钟房设备验收。

后续检查修正了同设备多条 TCP 连接的状态覆盖：网关统一汇总连接，桌面控制器和命令行使用相同结果。只要还有未被协议拒绝的连接，旧连接关闭或其他连接协议失败均不将设备覆盖为离线。全部连接关闭后保留本轮协议错误供排查，新连接建立时重置该错误；connected 仅表示 TCP 连接，不代表厂商业务授权成功。

新增真实 TCP 回归覆盖旧连接关闭、并存连接异常、健康连接继续回复、最后连接关闭、重连恢复及唯一连接异常。网关测试 29 项通过。

同时修正读取原厂缓存期间停止网关的竞态：迟到缓存结果不得创建监听或遗留运行实例，测试验证停止后可以重新启动。

网关云端客户端增加生命周期取消：桌面停止时取消授权验证及在途查询/心跳，独立网关监听关闭时同样取消云端请求。授权验证测试保持服务端不返回响应，在两秒测试期限内验证停止、启动承诺结束和再次启动，避免依赖原五秒 HTTP 超时。29 项网关测试及客户端类型检查通过；尚未发布或实机验收。

进一步执行客户端完整构建成功（main、preload、renderer）。检查确认 App 按身份 epoch 重建页面，transport 丢弃旧身份响应，并在门店/身份变化时停止本机网关；此项为代码检查，未代替浏览器交互验收。

设备配置与网关数据库集成测试未完成：首次未加载测试环境；加载 .runtime/test.env 后，独立探测确认隔离测试数据库 127.0.0.1:15433/za_spa_saas_test 返回 ECONNREFUSED。测试进程已停止，没有切换生产数据库运行测试。待恢复测试数据库连接后重跑 tests/device-quick-setup.test.ts 和 tests/hardware-gateway.test.ts。

后续已恢复回环地址 SSH 隧道，使用原隔离测试数据库完成补验：device-quick-setup 集成测试通过。hardware-gateway 首次发现旧断言仍要求只读房态一律拒绝，与当前已实现的日志支持的只读映射矛盾；改为断言真实空闲房态、占用房态和指定技师/项目，同时验证 ReportClock 仍拒绝，撤销授权后房态接口返回 401、处理器拒绝。更新后的网关数据库集成测试通过，原有跨商家、跨门店、停用和配对权限断言均保留。此结果证明云端测试业务数据到网关的只读映射，不证明物理设备兼容。

增加停止/重启时旧心跳隔离回归：服务端挂起旧心跳并预设 401；停止后旧检查立即结束，再启动新网关，释放旧 401 后确认新网关仍 running 且新授权检查成功。控制器 5 项测试通过。此次只增加验证，没有放开未知厂商业务协议。

再次核对菜单发送类型：源码误将 menu 返回为 JSON 字符串，与已留存厂商静态证据冲突。.runtime/hardware-disassembly.json 中 AnswerOneWaySuccessForDeviceLogin 先 Marshal，0x92289a 调用 strings.Replace，0x922a47 读取替换后缓冲并发送，而日志 0x9229f5 读取替换前缓冲。结合此前常量核对，实际菜单应为数组。已恢复数组类型，并在真实本机 TCP 登录应答解码后断言菜单内容及数组类型。全部 30 项网关测试通过；合成菜单不代表取得了真实门店菜单或实机验证。
