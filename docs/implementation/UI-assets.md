# UI assets

用户提供 AYRA0–AYRA3 作为大致视觉参考，未将参考截图直接用作界面。

- Logo：用户提供的 ZENTRA-Web-App-Icons.zip 中 zentra-nav-charcoal.svg，原样用于浅色侧栏。
- App icon：同包 zentra-app-porcelain，应用于 Web、Tauri、Expo。产品名称仍为 AYRA。
- Landscape：使用 ImageGen 根据参考图生成文字空白的晨雾山湖照片，2172 × 724。供桌面 Hero 和侧栏使用；原生窗口不再绘制模拟窗口外框。
- UI icons：@phosphor-icons/react；使用包内标准图标。
- Fonts：系统 -apple-system / Segoe UI / sans-serif 与 Georgia / serif。

这些是当前用户指定的界面素材，不改变白皮书的业务边界和未定商业参数。

- Website product image：ImageGen 生成的银色 MacBook，1536 × 1024；标注 Product vision，仅为产品示意，不能作为功能交付截图。
