# M4 — Agent Runtime & Model Gateway

状态：进行中，未通过 Definition of Done。

- 已定义 AYRA 自有 `AgentRuntime`、`ModelProvider` 接口及带来源的 Context、Capability、Budget、ApprovalDecision 类型。供应商 SDK 类型不进入 Domain。
- 子 Agent 委派前可检查 Capability 和资源集合是父级授权的子集；空资源集合不会被当作全局授权。
- 模型选择先按 capability、region、privacy、context、availability、预算过滤，再用 Auto / Fast / Balanced / Deep 偏好排序。测试覆盖路由偏好及无合格模型时拒绝。
- 尚未实现 OpenAI Agents SDK adapter、真实模型调用、持久 checkpoint、EvalRunner 与 Golden cases、模型/提示版本的持久记录。生产 Worker 仍未接入 AgentRuntime；Task 执行门控保持关闭。
