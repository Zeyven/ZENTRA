# ZA Thera｜澜序 1.0.16

2026-09-18 发布 Web、API 和 Windows stable/rc 更新渠道。目录：`/opt/za-spa-saas/releases/20260918-v1.0.16`；上一目录 `/opt/za-spa-saas/releases/20260915T094000Z` 保留。本次无数据库迁移，没有改动旧服务或资源限制；旧 zuyu 服务发布前已 inactive。

修正空正文设备心跳、未知钟状态误映射、多 TCP 连接状态覆盖、停止期间迟到启动和在途请求取消。根据厂商最终发送路径恢复登录菜单数组类型，纠正 1.0.15 发布说明把日志占位字符串视为网络类型的错误。

验证：30 项网关测试、客户端类型检查、完整构建、Windows 打包通过；前序两项隔离数据库集成测试通过。安装包内版本和网关修复已检查，公网下载完整文件 SHA256 与本机构建一致。首页、平台入口、stable 和 rc 清单均 HTTP 200，公网首页脚本内容哈希与本机构建一致；线上 ready 返回 1.0.16 且任务健康。

安装包 SHA256：`cbf317b40686e70701b4972e6e304f7dda00b17e94607d66ad3067c09827bc6b`，112976595 字节。

本次未重做交互式安装/客户端实际自动升级验收，也未完成真实面板或播报器验收。仍只开放受限登录准备及只读房态；报钟业务写入与厂商播报协议未开放。
