# V1.0.9 设备检测与配置发布

2026-09-15，按用户授权同步发布 Web、API、Windows stable 与 rc 更新渠道。

发布目录：/opt/za-spa-saas/releases/20260915T011440Z。前版本目录：/opt/za-spa-saas/releases/20260914T083615Z。无新增数据库迁移；发布前备份 /opt/za-spa-saas/backups/pre-1.0.9.dump 的 pg_restore --list 通过，未进行完整恢复演练。旧 zuyu 服务保持 inactive，SaaS 配额保持 768 MiB / CPU 60%。

## 发布内容
- Windows 本机只读设备枚举、串口与局域网信息、端口占用检查、重复检测差异提示。
- 设备候选选择、配置预览、刷牌器和播报器批量原子保存、并发版本保护与幂等。
- 多面板房间绑定与授权，以及桌面内直接启动/停止只读网关；切账号、门店时停止监听。
- Web 同步配置界面。本机扫描依赖 Windows 客户端，浏览器不具备相同硬件权限。

## 本轮验证
- 类型检查，contracts/server/Web/Electron 构建，NSIS 打包通过。
- 17 项协议、控制器、发现与安全测试；3 项隔离数据库集成测试；1 项 Electron 网关操作测试通过。
- 包内 31 个文件与构建字节一致；安装包更新 SHA512 一致。
- 公网首页与 Settings、TechnicianAnnouncer 资源逐字节匹配本地 Web 构建。
- stable/latest.yml、rc/rc.yml 均为 1.0.9；公网下载完整安装包 SHA256 匹配；固定下载别名大小匹配。
- API /ready 返回 1.0.9、任务健康；未认证网关管理请求 401；Nginx 配置检查通过。

安装包：ZA-Thera-1.0.9-Windows-x64.exe，112970495 字节。
SHA256：f8d982a39be122af8bc29be9895321f15e6eb7e559e145b7d444e1bf08258189。

## 未验证与限制
本轮没有重新执行实际 NSIS 安装、升级、卸载流程。安装包 Authenticode 状态 NotSigned。
未进行门店实机验收；设备注册、菜单业务映射、报钟写入和厂商远端播报协议仍缺证据，不能声称完整硬件接通或全自动配置完成。只读网关凭证仅保存在内存中，不承诺重启后自动恢复。冻结验收要求仍未全部完成。

## 回滚
保留 1.0.8 版本安装文件及前发布目录；stable/pre-1.0.9.yml 保存旧清单。服务出现回归时切回前发布目录并重启 SaaS；更新渠道应同步恢复旧 stable 清单、对应 rc 清单与下载别名。1.0.9 已安装用户不会自动降级，必要时发布修正版本。未覆盖旧项目。
