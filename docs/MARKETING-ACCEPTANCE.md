# 自动营销验收

2026-09-08。测试库迁移至 017；正式库仍为 016。六类工作流为新客未到店、消费后复购、生日、沉睡、到期、取消预约。首次读取只返回未启用草稿，不写示例经营配置。

按门店和业务周期生成唯一任务，事务内发放真实优惠券并标记完成。队列冻结券配置，配置后续变更不影响已排队权益。停用会员或工作流跳过；失败有分次重试与原因审计。短信、微信保留接口，未接入不能启用或伪造外发成功。

`tests/marketing.test.ts` 3/3 通过：六种触发 SQL 实际运行、并发只发一次、跨商家/门店隔离、版本冲突、失败后不落券、重试使用原券金额、冻结会员跳过。

设置页取消失焦自动保存，改为显式校验与保存；直接使用已落库返回值，结果未知锁定表单并按原幂等键重试。浏览器首轮用例误用“系统设置”导航名称（真实为“门店设置”）超时；修正定位后补验 1/1 通过，验证非法金额保留、双击只提交一次、丢响应后表单和关闭锁定、原键重试以及扫描落库一次。最终类型检查通过。

迁移首轮发现旧报表测试写入 `test-0/test-1` 虚构触发类型及零值优惠券；修正测试夹具，仅将测试库中精确匹配的 10 条夹具改为停用的合法触发类型后迁移通过，未放宽生产约束。
