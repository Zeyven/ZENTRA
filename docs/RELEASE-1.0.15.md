# ZA Thera｜澜序 1.0.15

2026-09-15，发布 API、Web 与 Windows 客户端 1.0.15。

- 依据当前固件真实登录日志，修正点钟王 `SystemDeviceLogin.data.menu` 为 JSON 字符串，避免将原厂资源菜单错误编码成数组。
- 增加离线响应结构提取器，只输出协议字段形状，不输出设备、门店或人员信息。
- 继续仅开放已验证的只读房态。钟务写入、技师身份校验及钟房播报仍未获得实机验收。

验证：点钟王网关协议与菜单测试 15/15 通过；证据 JSON 校验通过。无数据库迁移。
