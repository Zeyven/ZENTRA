# ZA Thera｜澜序 1.0.17

2026-09-18 已发布 API、Web、Windows stable 和 rc 更新渠道。

本版将门店业态保存失败反馈、防重复操作及保持当前门店的修复带入 Windows 客户端；共享操作按钮识别返回值形式的失败，并提示结果未知时核对原请求。发布工具新增逐项迁移检查与核心回归门禁。

发布前验证：全项目类型检查、32 项核心回归、30 项网关测试全部通过，无失败或跳过。生产数据库只读检查核对 35 项迁移以及相关字段、约束、索引通过。本次无新增数据库迁移。

发布目录 `/opt/za-spa-saas/releases/20260918-v1.0.17`；前一版本 `/opt/za-spa-saas/releases/20260918-v1.0.16` 保留。切换前再次执行数据库检查，服务失败或旧服务状态改变会回滚；更新文件校验后最后替换清单。旧 zuyu 服务发布前后均 inactive，未对其启停，也未修改资源额度。

公网首页、平台入口、stable/rc 清单均 HTTP 200，主网页脚本哈希与本机构建一致。API ready 返回 1.0.17、任务健康；公网完整下载安装包与本地 SHA256 一致：

`ffa4bbb0136f30078d9f9f9d593f63446b81a58eeae0e5251a54be130019ac8f`

安装包 112976760 字节。包内版本、门店保存修复、共享反馈已检查；更新 manifest SHA512 与安装包一致。

本次未重新执行交互式安装、用户客户端实际自动升级、完整浏览器交互或设备实机验收。未放开点钟王业务写入或厂商播报功能。
