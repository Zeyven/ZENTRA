# 当前版本全面回归检查

范围：rc.8，生产为独立 SaaS，新数据库 028；Windows 包 SHA-256 为 39769ba58137f40df9f5df164137d6167d7180da36d93c18af5784780828dc37。业务回归仅使用 za_spa_saas_test，生产不创建商家或交易。旧项目 93 项源文件哈希不变。

## 已复现并修复

畸形 JSON 请求原先返回 500 INTERNAL_ERROR，误导用户认为服务器故障，且进入通用错误日志。超出请求体上限的请求也被错误映射。现按 body-parser 的实际错误类型和状态分别返回 400 INVALID_JSON、413 REQUEST_TOO_LARGE，保留 256 KiB 输入上限，不回显请求内容，不记录解析器原始消息。新增真实 HTTP 回归 tests/request-errors.test.ts，修复前复现 500 != 400，修复后通过。

接口依据：https://expressjs.com/en/resources/middleware/body-parser/#errors 。此次仅服务端错误映射改变，不需要升级客户端或迁移数据库。

## 基础检查

类型检查通过；服务端重新构建通过；Windows 包 29 项内置文件、图标及更新清单哈希一致。生产依赖 npm audit --omit=dev 报告 0 项已知漏洞；此结果不等于不存在安全问题。

云端数据库、后台任务及备份监测健康。新 API 配额 768 MiB / 60% CPU、数据库 1 GiB / 60% CPU 保持不变；旧服务健康。Nginx 配置检查通过，TLS 链及域名验证通过，证书到期时间 2026-12-07；备份、备份监测与证书续期定时任务存在。

## 验证边界

此轮覆盖仓库自动化用例与相关源码复核，不能证明零缺陷。支付/团购及实体打印机、读卡器按既定范围仅预留接口，未做真实服务商或硬件联调。Windows 使用真实 Electron 启动与业务测试及安装包内部校验，本轮不重新执行 NSIS 安装后升级链路。容量和长时间运行需要与功能回归分别看待，不以功能用例数量宣称容量。

## 后端最终结果

既有完整回归 128/128 通过，用时 729.96 秒，无失败、取消或跳过。新增请求解析回归 1/1 通过，共 129 项。覆盖账号和商家隔离、数据库 RLS、授权撤销、支持会话、幂等与并发资金、退款反结账、钟务、会员权益、预约营销、库存采购调拨、交接班、报表工资、模板、导出和后台任务。

## 界面最终结果

36 项 Web/Electron 用例：首轮 35 项通过、1 项旧测试路径及状态文案过期；按当前平台商家管理入口与已过期状态修正，增加撤销按钮禁用断言，保留两个旧邀请激活失败的真实接口断言。该用例补测通过，最终 36 项全部覆盖通过，未跳过。首轮日志 full-browser-rc8.log、最终补测 invite-retest-final.log。覆盖首次开通到现金结账、实时双端、弱网重试、资产退款、截断导出、权限、门店、采购调拨、营销和模板。

容量测试脚本此前使用 48 位密码，未同步现行 8–20 位规则；改为 20 位随机密码，仅修正测试输入，不改变产品密码策略。

## 容量与上线复核

20 商家、100 门店、100 在线连接隔离容量测试通过：124.41 秒内 2800 次请求，P95 964.12ms，最大 1246.87ms，401 次实时事件、0 串商家事件、0 错误，最终 100 个连接保持在线。旧服务 62 次健康采样，P95 11.87ms。新测试 API 峰值内存 112058368 字节，资源配额保持不变。该短时负载接近 1 秒 P95 门槛，不能外推为更高容量或长期稳定性。

上线复核发现 Nginx 的 256k 请求体限制先于 Express 返回 HTML 413，使客户端不能读取 JSON 错误。仅新 SaaS 的 /api/ 增加专用 413 JSON 错误响应，保持大小限制；配置检查和新旧健康检查失败均回滚。公开 HTTPS 真实畸形 JSON 返回 400 INVALID_JSON，超大请求返回 413 REQUEST_TOO_LARGE，均已验证。

服务端发布目录 20260909T015655Z，版本仍 rc.8，Windows 安装包无需变化。最终生产平台六工作区、登录退出、公开更新清单和包大小、Web 构建一致性与运维健康检查通过。生产商家列表仍为空，未恢复已删除数据。
